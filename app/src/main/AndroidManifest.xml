<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OnlineFoodOrderingSystem"
        tools:targetApi="31">

        <!-- Set LoginPage as the launcher activity -->
        <activity android:name=".LoginPage" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other Activities -->
        <activity android:name=".HomePage" android:exported="true"/>
        <activity android:name=".RegisterPage" android:exported="true"/>
        <activity android:name=".OrdersPage" android:exported="true"/>
        <activity android:name=".MenuPage" android:exported="true"/>
        <activity android:name=".CartPage" android:exported="true"/>
        <activity android:name=".PaymentPage" android:exported="true"/>
        <activity android:name=".OrderConfirmationPage" android:exported="true"/>

    </application>


</manifest>
